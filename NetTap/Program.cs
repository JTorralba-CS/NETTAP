using Core;
using Server;
using System;
using System.Collections.Generic;
using System.Linq;

namespace NetTap
{
    class Program
    {

        static void Main(String[] Arguments)
        {
            Syntax.Check(Arguments, typeof(Program));
        }
    }
}
